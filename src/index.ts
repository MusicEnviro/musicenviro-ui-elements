// placeholder code, just to make sure typescript is working in vscode

import chalk from 'chalk';

function text(str: string): number {
	console.log(chalk.green(str));

	return 1;
}

text('Hello World!');
